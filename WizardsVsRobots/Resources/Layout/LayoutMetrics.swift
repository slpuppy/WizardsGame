import UIKit

enum LayoutMetrics {
    static let buttonTitleFontSize: CGFloat = 24
    static let cornerRadius: CGFloat = 15
    static let iconConfig = UIImage.SymbolConfiguration(pointSize: LayoutMetrics.buttonTitleFontSize, weight: .black)
}
